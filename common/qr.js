var parse = require("url-parse");
var qrMap = {

}
function resolveQr(s) {

	let parsed = parse(res.result);
	switch(parsed.protocol){
		case "bshop":
		//api
			break;
	}
	if (parsed.protocol === 'bshop'){
	}else if(parse
}


module.exports = {
	formatTime: formatTime,
	formatLocation: formatLocation,
	dateUtils: dateUtils,
	showToast: showToast,
	showTip: showTip
};
